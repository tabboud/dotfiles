1. Add the install script for linux (ubuntu)
2. Add command line args for the install.sh with usage
3. Lazy load all of the configs

4. Add an "outside mode"
    - Make iterm and vim colors white/light background
    - increase brightness all the way
    - increase the text font in most windows

